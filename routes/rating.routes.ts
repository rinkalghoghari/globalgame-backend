// import * as express from "express";
// import { addRating, getRatingStats,checkUserRating } from "../controllers/rating.controller";
// import { authMiddleware } from "../middleware/authMiddleware";

// const router = express.Router();

// router.post("/add",authMiddleware, addRating);
// router.get("/stats/:gameId",authMiddleware, getRatingStats);
// router.get("/check/:gameId",authMiddleware, checkUserRating);

// export default router;
